Module Rules

One HTML5 video instance.

One VideoTexture.

Never recreate the texture when switching modes.

Modes only replace the renderer.

Inside
↓

BackSide Sphere

Sphere
↓

FrontSide Sphere

Flat
↓

Plane

Playback state is shared across all modes.

Changing modes must never restart playback.